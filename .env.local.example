# Mixpanel
# https://mixpanel.com → 프로젝트 설정 → Project Token 복사
NEXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_token_here

# Sender (API 연동 방식 선택 시)
# https://app.sender.net → Settings → API → API Key
SENDER_API_KEY=your_sender_api_key_here
